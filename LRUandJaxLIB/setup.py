from setuptools import setup, find_packages

setup(
    name="LRU and JAX library",
    version="0.1.0",
    description="A local library used to calculated the LRU and JAX library.",
    packages=find_packages(),
    python_requires='==3.10.11',
)